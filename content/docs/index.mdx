---
title: "Documentação da CodeChat API"
description: "Contrato, guias e referência técnica da API."
---
Esta pasta descreve o contrato observado no código executável atual. A referência de máquina é o [OpenAPI 3.1](/api-reference); os guias abaixo explicam autenticação, validações, respostas e particularidades operacionais.

## Comece aqui

1. [Autenticação](/docs/authentication): token global, token da instância e regras de correspondência.
2. [Instâncias](/docs/instances): criação, listagem, consulta, rotação de token e exclusão.
3. [Conexões](/docs/connections): QR Code, pairing code, passkey, estado e logout.
4. [Mensagens](/docs/messages): mensagens comuns, fila assíncrona e opções compartilhadas.
5. [Envio em lote](/docs/message-batches): recurso Pro com fila persistente, múltiplas instâncias, recovery e progresso.
6. [Chats](/docs/chats): conta WhatsApp, leitura, arquivo, edição e exclusão.
7. [Mídia](/docs/media): download de mídia e upload antecipado.
8. [Grupos](/docs/groups): criação, participantes, convite, foto e saída.
9. [Webhooks](/docs/webhooks): configuração, eventos, envelopes e entrega.

## CodeChat API Go Pro

- [Endpoints Pro](/docs/pro-endpoints): pagamento, PIX, revisão de pedido, formulário, mensagens interativas e Message Batch.

`Pro` é uma classificação comercial solicitada para a documentação. O código auditado não possui claim, middleware, tabela ou feature flag de plano e não retorna `402 Payment Required`. Os recursos Pro por instância usam o JWT da instância; Message Batch usa o token global administrativo. O OpenAPI registra `x-codechat-plan: pro` e `x-codechat-plan-enforced: false` para não confundir classificação com bloqueio executável.

## Compatibilidade

- [Endpoints legados](/docs/legacy-endpoints): aliases antigos ainda ativos e seus substitutos atuais.
- [Erros](/docs/errors): envelope real, códigos HTTP e inconsistências conhecidas.
- [Variáveis de ambiente](/docs/environment).
- [Pareamento por Passkey](/docs/passkey-pairing).
- [Migrações](/docs/migrations).

## Convenções

- A base URL local padrão é `http://localhost:8084`.
- Nas rotas atuais, `{instance}` é o nome público da instância.
- Nas rotas legadas, `{instanceName}` tem a mesma finalidade.
- Parâmetros são mostrados com chaves no OpenAPI e com `:param` em exemplos de rotas.
- Campos JSON desconhecidos são rejeitados nos handlers que usam decodificação estrita; as exceções estão destacadas nos guias.
- Endpoints legados só usam `deprecated: true` no OpenAPI quando existe substituto atual equivalente.
